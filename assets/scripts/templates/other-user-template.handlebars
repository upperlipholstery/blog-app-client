<a><img class="profile-image" src="{{user.imageUrl}}"></a><span>{{user.email}}: joined {{user.createdAt}}</span>
<div class="bio">
  <h2>Bio:</h2>
  <p>{{user.bio}}</p>
</div>
<div class="account-stats">
  <p>Stats:</p><br>
  <p>Tomes: {{length user.tomes}}</p>
  <p>Likes received: {{user.numLikes}}</p>
  <p>Times Archived: {{user.numFavs}}</p>
  <p>Notes left: {{user.numNotes}}</p>
</div>
